const sayHi = (age: number, name: string, gender: string): string => {
  return `hello ${name}, you are ${age}, you are a ${gender}`;
};
console.log(sayHi(30, 'shsong', 'male'));

// feature somthing
export {};
