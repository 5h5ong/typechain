const age = '20',
  name = 'shsong',
  gender = 'male';

const sayHi = (age, name?, gender?) => {
  console.log(`hello ${name}, you are ${age}, you are a ${gender}`);
};
sayHi(age);

// feature somthing
export {};
